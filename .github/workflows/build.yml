name: Build APK

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build APK
        run: |
          cd V2rayNG
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon 2>&1 | grep -E "error:|Error|FAILED|Exception" | tail -30

      - name: Upload APK
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: exhxx78-vpn
          path: V2rayNG/app/build/outputs/apk/**/*.apk
          if-no-files-found: warn
